// @flow
import TimeFormatter from 'packages/evo-server/src/IRacing/Timing/TimeFormatter';

/**
 * @type {TimeFormatter} timeFormatter
 */
const timeFormatter = new TimeFormatter();

export { timeFormatter };
